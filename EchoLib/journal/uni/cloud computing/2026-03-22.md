---
tags:
  - daily
  - journal
journal: cloud-computing
journal-start-date: 2026-03-22
journal-section: daily
journal-date: 2026-03-22
---
```calendar-nav
```

### Sunday, 22 March 2026

# Database
PK = primary key
FK = foreign key
## User Service
### user
- table for all users

| column         | type      | postgresql type                               | notes                                                                                                                                         |
| -------------- | --------- | --------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| **user_id**    | int       | serial                                        | PK (auto increment in db)                                                                                                                     |
| **username**   | str       | varchar(255)                                  | unique and not null                                                                                                                           |
| **password**   | str       | text                                          | optional, since is null for OAuth users. IMP: backend must ensure a user non OAuth has password != null. Also must be a hash of the password. |
| **gender**     | str       | varchar(10)                                   | backend must enforce options: *M,F,NB or O*                                                                                                   |
| **age**        | int       | integer                                       |                                                                                                                                               |
| **created_at** | timestamp | timestamp or datetime (str with no time zone) | ```DEFAULT NOW()```                                                                                                                           |
| **updated_at** | timestamp | timestamp or datetime                         | ```DEFAULT NOW()```                                                                                                                           |
### user_oauth
- table to store users with OAuth2.0 and their tokens
- one‑to‑one → users
- considered that we only will accept OAuth2.0 for google accounts
    - if other providers must add *provider_name* 

| column            | type      | postgresql type       | notes                                                          |
| ----------------- | --------- | --------------------- | -------------------------------------------------------------- |
| **oauth_id**      | int       | serial                | PK (auto increment in db)                                      |
| **user_id**       | int       | integer               | FK to users tables, on delete cascade                          |
| **provider_id**   | str       | varchar(255)          | not null                                                       |
| **refresh_token** | str       | text                  | optional column (if it justifies having it). should be hashed. |
| **expires_at**    | timestamp | timestamp or datetime | when token expires                                             |
| **created_at**    | timestamp | timestamp or datetime | `DEFAULT NOW()`                                                |
| **updated_at**    | timestamp | timestamp or datetime | `DEFAULT NOW()`                                                |
### user_sessions
- table to store user current sessions
- one-to-one -> users
    - on login → delete old session → create new one
    - if supports multiple devices logins then maybe many-to-one (1 user to many sessions)

| column            | type      | postgresql type       | notes                                 |
| ----------------- | --------- | --------------------- | ------------------------------------- |
| **session_id**    | str       | varchar(255) or UUID  | PK                                    |
| **user_id**       | int       | integer               | FK to users tables, on delete cascade |
| **refresh_token** | str       | text                  | Should be hashed, not null            |
| **created_at**    | timestamp | timestamp or datetime | `DEFAULT NOW()`                       |
| **updated_at**    | timestamp | timestamp or datetime | `DEFAULT NOW()`                       |
| **expires_at**    | timestamp | timestamp or datetime | when token expires                    |
| **revoked**       | bool      | boolean               | for jwt to logout                     |

## Movie Catalog
### movies
- table to store movies

| column              | type | postgresql type | notes                          |
| ------------------- | ---- | --------------- | ------------------------------ |
| **movie_id**        | int  | serial          | PK (auto increment in db)      |
| **movie_title**     | str  | varchar(255)    | not null                       |
| **description**     | str  | text            |                                |
| **imbd_url**        | str  | text            |                                |
| **release_year**    | int  | integer         | `CHECK (release_year >= 1888)` |
| **runtime**         | int  | integer         |                                |
| **parental_rating** | str  | varchar(20)     | e.g. PG-13                     |
| **poster_url**      | str  | text            | image poster                   |
| **is_deleted**      | bool | boolean         | `DEFAULT FALSE`                |
### genres
- table for genres available

| column       | type | postgresql type | notes                     |
| ------------ | ---- | --------------- | ------------------------- |
| **genre_id** | int  | serial          | PK (auto increment in db) |
| **name**     | str  | varchar(100)    | unique and not null       |
### movie_genres
- stores movies genres
- many-to-many -> movies, many-to-many -> genres

| column       | type | postgresql type | notes            |
| ------------ | ---- | --------------- | ---------------- |
| **genre_id** | int  | integer         | PK, FK to genres |
| **movie_id** | int  | integer         | PK, FK to movies |
### movie_cast
- stores movie cast of movie
- many‑to‑one → movies

| column        | type | postgresql type | notes                                       |
| ------------- | ---- | --------------- | ------------------------------------------- |
| **cast_id**   | int  | serial          | PK (auto increment in db)                   |
| **movie_id**  | int  | serial          | FK movies                                   |
| **cast_name** | str  | varchar(255)    | person's name                               |
| **role**      | str  | varcha(50)      | e.g. main actor, supporting actor, director |
## Review System
### ratings
- many‑to‑one → users, many‑to‑one → movies

| column             | type      | postgresql type       | notes                                                          |
| ------------------ | --------- | --------------------- | -------------------------------------------------------------- |
| **rating_id**      | int       | serial                | PK (auto increment in db)                                      |
| **user_id**        | int       | integer               | FK users                                                       |
| **movie_id**       | int       | integer               | FK movies                                                      |
| **rating**         | int       | integer               | `CHECK (rating BETWEEN 1 AND 5)`                               |
| **review**         | str       | text                  |                                                                |
| **tag**            | str       | text                  | for now just a list of movie tags (no separate table for tags) |
| **created_at**     | timestamp | timestamp or datetime | `DEFAULT NOW()`                                                |
| **updated_at**     | timestamp | timestamp or datetime | `DEFAULT NOW()`                                                |
| **is_quarantined** | bool      | boolean               | `DEFAULT FALSE` (see if default should be this)                |
### fraud_alerts
- many‑to‑one -> users,  many‑to‑many -> movies, many-to-many -> ratings

| column       | type      | postgresql type       | notes                                             |
| ------------ | --------- | --------------------- | ------------------------------------------------- |
| **alert_id** | int       | serial                | PK (auto increment in db)                         |
| **user_id**  | int       | integer               | FK users                                          |
| **rating_id**    | int       | integer               | FK ratings                                        |
| **movie_id**     | int       | integer               | FK movies                                         |
| **severity**     | str       | varchar(10)           | should be *low, medium or high*                   |
| **trigger**      | str       | varchar(255)          | e.g *rapid_rating, review_spam, suspicious_score* |
| **created_at**   | timestamp | timestamp or datetime | `DEFAULT NOW()`                                   |


# Update
- [ ] swagger.yaml with new schemas/endpoints
- [ ] remove from functional requirements - streaming platforms in movies
- [ ] 





## **4. Badges Relationships**

|Table|Relationship|
|---|---|
|**badges**|parent table|
|**user_badges**|many‑to‑one → users, many‑to‑one → badges|

**Meaning:** Users can earn many badges.

## **5. Watchlist Relationships**

|Table|Relationship|
|---|---|
|**watchlists**|many‑to‑one → users|
|**watchlist_items**|many‑to‑one → watchlists, many‑to‑one → movies|

**Meaning:** A user can have multiple watchlists; each watchlist contains many movies.

## **6. Subscription Relationships**

|Table|Relationship|
|---|---|
|**subscriptions**|many‑to‑one → users|

## **7. Recommendation System Relationships**

|Table|Relationship|
|---|---|
|**user_preferences**|many‑to‑one → users|
|**user_reference_movies**|many‑to‑one → users, many‑to‑one → movies|
|**genre_families**|parent table|
|**genre_family_members**|many‑to‑one → genre_families, many‑to‑one → genres|

## **8. Analytics Relationships**

| Table                | Relationship                                |
| -------------------- | ------------------------------------------- |
| **review_sentiment** | one‑to‑one → ratings                        |
| **topics**           | parent table                                |
| **rating_topics**    | many‑to‑one → ratings, many‑to‑one → topics |
| **user_clusters**    | one‑to‑one → users                          |