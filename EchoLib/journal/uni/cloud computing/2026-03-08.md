---
tags:
  - daily
  - journal
journal: cloud-computing
journal-start-date: 2026-03-08
journal-section: daily
journal-date: 2026-03-08
---
```calendar-nav
```

### Sunday, 08 March 2026
## Phase 2
TO DO:
- add people to rep
- cleanup dataset


Remember:
``
```
echo "# CC2526-Group8" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin git@github.com:jcarrasqueira/CC2526-Group8.git
git push -u origin main
```

```
git remote add origin git@github.com:jcarrasqueira/CC2526-Group8.git
git branch -M main
git push -u origin main
```


# Schema

## Users (get add filters)
- user_id (prim key) - auto increment
- username
- email
- gender
- age
- password hash

## User auth
## User Preferences
- user_id
- topics (list genres) -> alternative multiple entries

## Movies
- movie_id
- movie_title
- genres
- description
- imbd url
- release_year
- tags (top tags) `If needed movie tags/genres tables`

## Rating
- rating_id
- user_id
- movie_id
- rating (`rating INT CHECK (rating BETWEEN 1 AND 5),`)
- review
- timestamp
- tag
## Rating evaluation
- rating_id
- valid

## Watchlist
confirm if it mkes sense
- prim key - watclist id
- dict( userid, movies)
- title
## Watchlist Items
```sql
CREATE TABLE watchlists (
    watchlist_id SERIAL PRIMARY KEY,
    user_id INT NOT NULL REFERENCES users(user_id) ON DELETE CASCADE,
    title VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);
```

```sql
CREATE TABLE watchlist_items (
    watchlist_id INT NOT NULL REFERENCES watchlists(watchlist_id) ON DELETE CASCADE,
    movie_id INT NOT NULL REFERENCES movies(movie_id) ON DELETE CASCADE,
    added_at TIMESTAMP DEFAULT NOW(),
    PRIMARY KEY (watchlist_id, movie_id)
);
```

## Subscription
- user_id
- type
- start_date
- end_date

## Badges
- user_id
- badge_id
- milestone
- title

## User Badges
- user_id
- badge
- milestone
- awarded_at


# Endpoints
```
/users
/users/{userId}
/auth/login
/auth/refresh
/auth/logout
/movies
/movies/{movieId}
/ratings
/ratings/{ratingId}
/users/{userId}/ratings
/badges
/badges/{badgeId}
/users/{userId}/badges
/watchlists
/watchlists/{watchlistId}
/users/{userId}/watchlist
/subscriptions
/subscriptions/{subscriptionId}
```
## User
- get users
- get user
- post user
- delete user
- update user
## User Login
- auth part

## Movies
- get movies
- get movies
- post movie
- update movie
- delete movie

## Rating
- get ratings (filters)
- get rating (by user)
- get rating
- post rating
- delete rating
- update rating

## Badges
- get badges
- get user badges
- post bagdes
- delete badges 
- update badges

## Watchlist
- get watchlists
- get user watchlists
- post watchlist
- update watchlist
- delete watchlist

## Subscriptions
- get subscription
- post subscription
- update subscription 
- delete subscription (confirmation)

**Still not sure about struct**
## User Preferences
- get user preferences
## Recommendations 
- get recommendations (by user)
